<UserControl x:Class="OpenUtau.UI.Controls.NavigateDrag"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="20"
             MouseLeftButtonDown="NavigateDrag_MouseLeftButtonDown" MouseLeftButtonUp="NavigateDrag_MouseLeftButtonUp"
             MouseMove="NavigateDrag_MouseMove" MouseEnter="UserControl_MouseEnter"  MouseLeave="UserControl_MouseLeave">
    <UserControl.Template>
        <ControlTemplate TargetType="UserControl">
            <StackPanel Background="{TemplateBinding Background}" SnapsToDevicePixels="True" Focusable="False">
                <Path Name="PATH" Stretch="None" Margin="2,2,2,2" Fill="{TemplateBinding Foreground}"
                      Data="M 0 8 L 3 11 L 3 5 Z M 8 0 L 5 3 L 11 3 Z M 16 8 L 13 11 L 13 5 Z M 8 16 L 5 13 L 11 13 Z"/>
            </StackPanel>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
